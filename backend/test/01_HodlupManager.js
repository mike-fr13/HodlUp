const { loadFixture } = require('@nomicfoundation/hardhat-network-helpers')
const { expect } = require('chai')
const { BigNumber } = require('ethers')
const { anyValue } = require('@nomicfoundation/hardhat-chai-matchers/withArgs');
const Constant = require("./lib/Constants.js")
const Fixture = require("./lib/Fixtures.js")

describe('HodlupManager', function () {

    describe("test constructor", function () {
        it("should initialize", async () => {
        });
    });

    describe("test createDcaPaire", function () {
    });

    describe("test withdrawAllFees", function () {
    });

    describe("test executeAllSwap", function () {
    });
    
});